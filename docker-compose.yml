services:
  jellyfin:
    image: jellyfin/jellyfin:${IMAGE_TAG:-latest}
    container_name: ${CONTAINER_NAME:-jellyfin}
    user: "${PUID:-1000}:${PGID:-1000}"
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - ./config:/config
      - ./cache:/cache
      - ${MEDIA_PATH:-/media}:/media:ro
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      # HTTPS Router
      - "traefik.http.routers.${SERVICE_NAME:-jellyfin}.rule=Host(`${SERVICE_DOMAIN:-jellyfin.roadtotech.me}`)"
      - "traefik.http.routers.${SERVICE_NAME:-jellyfin}.entrypoints=${HTTPS_ENTRYPOINT:-websecure}"
      - "traefik.http.routers.${SERVICE_NAME:-jellyfin}.tls=true"
      - "traefik.http.routers.${SERVICE_NAME:-jellyfin}.tls.certresolver=${CERT_RESOLVER:-myresolver}"
      - "traefik.http.routers.${SERVICE_NAME:-jellyfin}.middlewares=${SERVICE_NAME:-jellyfin}-ratelimit@docker"
      - "traefik.http.routers.${SERVICE_NAME:-jellyfin}.service=${SERVICE_NAME:-jellyfin}-svc"

      # Rate Limit Middleware
      - "traefik.http.middlewares.${SERVICE_NAME}-ratelimit.ratelimit.average=${RATE_LIMIT_AVG:-100}"
      - "traefik.http.middlewares.${SERVICE_NAME}-ratelimit.ratelimit.burst=${RATE_LIMIT_BURST:-50}"

      # HTTP to HTTPS Redirect
      - "traefik.http.routers.${SERVICE_NAME}-red.rule=Host(`${SERVICE_DOMAIN}`)"
      - "traefik.http.routers.${SERVICE_NAME}-red.entrypoints=${HTTP_ENTRYPOINT:-web}"
      - "traefik.http.routers.${SERVICE_NAME}-red.middlewares=https-redirect@docker"

      # Service definition
      - "traefik.http.services.${SERVICE_NAME}-svc.loadbalancer.server.port=8096"

networks:
  proxy:
    name: ${PROXY_NETWORK:-proxy-net}
    external: true
